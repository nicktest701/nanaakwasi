import React from "react";

function Loading() {
  return (
    <section className="fixed inset-0 bg-secondary-900 flex items-center justify-center">
      <div className="loader"></div>
    </section>
  );
}

export default Loading;
